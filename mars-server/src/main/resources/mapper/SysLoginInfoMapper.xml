<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mars.admin.modules.system.mapper.SysLoginInfoMapper">

    <!-- 登录日志基础信息结果映射 -->
    <resultMap id="SysLoginInfoResult" type="com.mars.admin.modules.system.entity.SysLoginInfo">
        <id property="id" column="id"/>
        <result property="userName" column="user_name"/>
        <result property="ipaddr" column="ipaddr"/>
        <result property="loginLocation" column="login_location"/>
        <result property="browser" column="browser"/>
        <result property="os" column="os"/>
        <result property="status" column="status"/>
        <result property="msg" column="msg"/>
        <result property="loginTime" column="login_time"/>
    </resultMap>

    <!-- 分页查询登录日志列表 -->
    <select id="selectLoginInfoList" parameterType="com.mars.admin.modules.system.entity.SysLoginInfo" resultMap="SysLoginInfoResult">
        SELECT
            li.id,
            li.user_name,
            li.ipaddr,
            li.login_location,
            li.browser,
            li.os,
            li.status,
            li.msg,
            li.login_time
        FROM sys_logininfor li
        WHERE 1=1
        <if test="userName != null and userName != ''">
            AND li.user_name LIKE CONCAT('%', #{userName}, '%')
        </if>
        <if test="ipaddr != null and ipaddr != ''">
            AND li.ipaddr LIKE CONCAT('%', #{ipaddr}, '%')
        </if>
        <if test="status != null and status != ''">
            AND li.status = #{status}
        </if>
        ORDER BY li.login_time DESC
    </select>

    <!-- 清空登录日志 -->
    <delete id="cleanLoginInfo">
        DELETE FROM sys_logininfor
    </delete>

</mapper>
