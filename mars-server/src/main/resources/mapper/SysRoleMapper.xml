<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mars.admin.modules.system.mapper.SysRoleMapper">

    <!-- 角色基础信息结果映射 -->
    <resultMap id="SysRoleResult" type="com.mars.admin.modules.system.entity.SysRole">
        <id property="id" column="role_id"/>
        <result property="roleName" column="role_name"/>
        <result property="roleCode" column="role_code"/>
        <result property="roleKey" column="role_key"/>
        <result property="roleSort" column="role_sort"/>
        <result property="dataScope" column="data_scope"/>
        <result property="menuCheckStrictly" column="menu_check_strictly"/>
        <result property="deptCheckStrictly" column="dept_check_strictly"/>
        <result property="status" column="status"/>
        <result property="isSystem" column="is_system"/>
        <result property="description" column="description"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="deleteTime" column="delete_time"/>
    </resultMap>

    <!-- 分页查询角色列表 -->
    <select id="selectRoleList" parameterType="com.mars.admin.modules.system.entity.SysRole" resultMap="SysRoleResult">
        SELECT
            r.id as role_id,
            r.role_name,
            r.role_code,
            r.role_key,
            r.role_sort,
            r.data_scope,
            r.menu_check_strictly,
            r.dept_check_strictly,
            r.status,
            r.is_system,
            r.description,
            r.remark,
            r.create_time,
            r.update_time,
            r.create_by,
            r.update_by,
            r.is_deleted,
            r.delete_time
        FROM sys_role r
        WHERE r.is_deleted = 0
        <if test="roleName != null and roleName != ''">
            AND r.role_name LIKE CONCAT('%', #{roleName}, '%')
        </if>
        <if test="roleCode != null and roleCode != ''">
            AND r.role_code LIKE CONCAT('%', #{roleCode}, '%')
        </if>
        <if test="roleKey != null and roleKey != ''">
            AND r.role_key LIKE CONCAT('%', #{roleKey}, '%')
        </if>
        <if test="status != null">
            AND r.status = #{status}
        </if>
        ORDER BY r.role_sort ASC, r.create_time DESC
    </select>

    <!-- 根据用户ID查询角色列表 -->
    <select id="selectRolesByUserId" parameterType="Long" resultMap="SysRoleResult">
        SELECT
            r.id as role_id,
            r.role_name,
            r.role_code,
            r.role_key,
            r.role_sort,
            r.data_scope,
            r.menu_check_strictly,
            r.dept_check_strictly,
            r.status,
            r.is_system,
            r.description,
            r.remark,
            r.create_time,
            r.update_time,
            r.create_by,
            r.update_by,
            r.is_deleted,
            r.delete_time
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE r.is_deleted = 0 AND r.status = 1 AND ur.user_id = #{userId}
        ORDER BY r.role_sort ASC
    </select>

    <!-- 查询角色详情（包含菜单、部门信息） -->
    <select id="selectRoleDetailById" parameterType="Long" resultMap="SysRoleResult">
        SELECT
            r.id as role_id,
            r.role_name,
            r.role_code,
            r.role_key,
            r.role_sort,
            r.data_scope,
            r.menu_check_strictly,
            r.dept_check_strictly,
            r.status,
            r.is_system,
            r.description,
            r.remark,
            r.create_time,
            r.update_time,
            r.create_by,
            r.update_by,
            r.is_deleted,
            r.delete_time
        FROM sys_role r
        WHERE r.id = #{roleId} AND r.is_deleted = 0
    </select>

    <!-- 根据菜单ID查询角色列表 -->
    <select id="selectRolesByMenuId" parameterType="Long" resultMap="SysRoleResult">
        SELECT
            r.id as role_id,
            r.role_name,
            r.role_code,
            r.role_key,
            r.role_sort,
            r.data_scope,
            r.menu_check_strictly,
            r.dept_check_strictly,
            r.status,
            r.is_system,
            r.description,
            r.remark,
            r.create_time,
            r.update_time,
            r.create_by,
            r.update_by,
            r.is_deleted,
            r.delete_time
        FROM sys_role r
        INNER JOIN sys_role_menu rm ON r.id = rm.role_id
        WHERE r.is_deleted = 0 AND r.status = 1 AND rm.menu_id = #{menuId}
        ORDER BY r.role_sort ASC
    </select>

</mapper>
