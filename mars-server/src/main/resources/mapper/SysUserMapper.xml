<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mars.admin.modules.system.mapper.SysUserMapper">

    <!-- 用户基础信息结果映射 -->
    <resultMap id="SysUserResult" type="com.mars.admin.modules.system.entity.SysUser">
        <id property="id" column="user_id"/>
        <result property="username" column="username"/>
        <result property="nickname" column="nickname"/>
        <result property="realName" column="real_name"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="avatar" column="avatar"/>
        <result property="gender" column="gender"/>
        <result property="birthday" column="birthday"/>
        <result property="status" column="status"/>
        <result property="userType" column="user_type"/>
        <result property="loginCount" column="login_count"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="lastLoginIp" column="last_login_ip"/>
        <result property="passwordUpdateTime" column="password_update_time"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="deleteTime" column="delete_time"/>
        <!-- 添加角色ID列表属性映射 -->
        <collection property="roleIds" ofType="java.lang.Long" javaType="java.util.ArrayList">
            <result column="role_id"/>
        </collection>
        <!-- 添加岗位ID列表属性映射 -->
        <collection property="postIds" ofType="java.lang.Long" javaType="java.util.ArrayList">
            <result column="post_id"/>
        </collection>
        <!-- 添加部门ID列表属性映射 -->
        <collection property="deptIds" ofType="java.lang.Long" javaType="java.util.ArrayList">
            <result column="dept_id"/>
        </collection>
    </resultMap>

    <!-- 查询用户详情（包含角色、部门、岗位信息） -->
    <select id="selectUserDetailById" parameterType="Long" resultMap="SysUserResult">
        SELECT
            u.id as user_id,
            u.username,
            u.nickname,
            u.real_name,
            u.email,
            u.phone,
            u.avatar,
            u.gender,
            u.birthday,
            u.status,
            u.user_type,
            u.login_count,
            u.last_login_time,
            u.last_login_ip,
            u.password_update_time,
            u.remark,
            u.create_time,
            u.update_time,
            u.create_by,
            u.update_by,
            u.is_deleted,
            u.delete_time,
            ur.role_id,
            up.post_id,
            ud.dept_id
        FROM sys_user u
        LEFT JOIN sys_user_role ur ON u.id = ur.user_id
        LEFT JOIN sys_user_post up ON u.id = up.user_id
        LEFT JOIN sys_user_dept ud ON u.id = ud.user_id
        WHERE u.id = #{userId} AND u.is_deleted = 0
    </select>

    <!-- 分页查询用户列表 -->
    <select id="selectUserList" parameterType="com.mars.admin.modules.system.entity.SysUser" resultMap="SysUserResult">
        SELECT
            u.id as user_id,
            u.username,
            u.nickname,
            u.real_name,
            u.email,
            u.phone,
            u.avatar,
            u.gender,
            u.birthday,
            u.status,
            u.user_type,
            u.login_count,
            u.last_login_time,
            u.last_login_ip,
            u.password_update_time,
            u.remark,
            u.create_time,
            u.update_time,
            u.create_by,
            u.update_by,
            u.is_deleted,
            u.delete_time
        FROM sys_user u
        WHERE u.is_deleted = 0
        <if test="username != null and username != ''">
            AND u.username LIKE CONCAT('%', #{username}, '%')
        </if>
        <if test="nickname != null and nickname != ''">
            AND u.nickname LIKE CONCAT('%', #{nickname}, '%')
        </if>
        <if test="phone != null and phone != ''">
            AND u.phone LIKE CONCAT('%', #{phone}, '%')
        </if>
        <if test="status != null">
            AND u.status = #{status}
        </if>
        <if test="userType != null">
            AND u.user_type = #{userType}
        </if>
        ORDER BY u.create_time DESC
    </select>

    <!-- 根据角色ID查询用户列表 -->
    <select id="selectUsersByRoleId" parameterType="Long" resultMap="SysUserResult">
        SELECT
            u.id as user_id,
            u.username,
            u.nickname,
            u.real_name,
            u.email,
            u.phone,
            u.avatar,
            u.gender,
            u.birthday,
            u.status,
            u.user_type,
            u.login_count,
            u.last_login_time,
            u.last_login_ip,
            u.password_update_time,
            u.remark,
            u.create_time,
            u.update_time,
            u.create_by,
            u.update_by,
            u.is_deleted,
            u.delete_time
        FROM sys_user u
        INNER JOIN sys_user_role ur ON u.id = ur.user_id
        WHERE u.is_deleted = 0 AND ur.role_id = #{roleId}
        ORDER BY u.create_time DESC
    </select>

    <!-- 根据部门ID查询用户列表 -->
    <select id="selectUsersByDeptId" parameterType="Long" resultMap="SysUserResult">
        SELECT
            u.id as user_id,
            u.username,
            u.nickname,
            u.real_name,
            u.email,
            u.phone,
            u.avatar,
            u.gender,
            u.birthday,
            u.status,
            u.user_type,
            u.login_count,
            u.last_login_time,
            u.last_login_ip,
            u.password_update_time,
            u.remark,
            u.create_time,
            u.update_time,
            u.create_by,
            u.update_by,
            u.is_deleted,
            u.delete_time
        FROM sys_user u
        INNER JOIN sys_user_dept ud ON u.id = ud.user_id
        WHERE u.is_deleted = 0 AND ud.dept_id = #{deptId}
        ORDER BY u.create_time DESC
    </select>

    <!-- 更新用户登录信息 -->
    <update id="updateUserLoginInfo">
        UPDATE sys_user
        SET login_count = IFNULL(login_count, 0) + 1,
            last_login_time = NOW(),
            last_login_ip = #{loginIp}
        WHERE id = #{userId}
    </update>

</mapper>
