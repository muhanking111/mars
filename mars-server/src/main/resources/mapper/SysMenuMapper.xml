<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mars.admin.modules.system.mapper.SysMenuMapper">

    <!-- 菜单基础信息结果映射 -->
    <resultMap id="SysMenuResult" type="com.mars.admin.modules.system.entity.SysMenu">
        <id property="id" column="menu_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="menuName" column="menu_name"/>
        <result property="menuCode" column="menu_code"/>
        <result property="menuType" column="menu_type"/>
        <result property="routeName" column="route_name"/>
        <result property="routePath" column="route_path"/>
        <result property="component" column="component"/>
        <result property="redirect" column="redirect"/>
        <result property="queryParam" column="query_param"/>
        <result property="isFrame" column="is_frame"/>
        <result property="isCache" column="is_cache"/>
        <result property="menuTypeFlag" column="menu_type_flag"/>
        <result property="visible" column="visible"/>
        <result property="status" column="status"/>
        <result property="perms" column="perms"/>
        <result property="icon" column="icon"/>
        <result property="orderNum" column="order_num"/>
        <result property="isSystem" column="is_system"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="deleteTime" column="delete_time"/>
    </resultMap>

    <!-- 分页查询菜单列表 -->
    <select id="selectMenuList" parameterType="com.mars.admin.modules.system.entity.SysMenu" resultMap="SysMenuResult">
        SELECT
            m.id as menu_id,
            m.parent_id,
            m.menu_name,
            m.menu_code,
            m.menu_type,
            m.route_name,
            m.route_path,
            m.component,
            m.redirect,
            m.query_param,
            m.is_frame,
            m.is_cache,
            m.menu_type_flag,
            m.visible,
            m.status,
            m.perms,
            m.icon,
            m.order_num,
            m.is_system,
            m.remark,
            m.create_time,
            m.update_time,
            m.create_by,
            m.update_by,
            m.is_deleted,
            m.delete_time
        FROM sys_menu m
        WHERE m.is_deleted = 0
        <if test="menuName != null and menuName != ''">
            AND m.menu_name LIKE CONCAT('%', #{menuName}, '%')
        </if>
        <if test="menuCode != null and menuCode != ''">
            AND m.menu_code LIKE CONCAT('%', #{menuCode}, '%')
        </if>
        <if test="menuType != null and menuType != ''">
            AND m.menu_type = #{menuType}
        </if>
        <if test="status != null">
            AND m.status = #{status}
        </if>
        <if test="parentId != null">
            AND m.parent_id = #{parentId}
        </if>
        ORDER BY m.parent_id ASC, m.order_num ASC
    </select>

    <!-- 根据用户ID查询菜单列表 -->
    <select id="selectMenusByUserId" parameterType="Long" resultMap="SysMenuResult">
        SELECT DISTINCT
            m.id as menu_id,
            m.parent_id,
            m.menu_name,
            m.menu_code,
            m.menu_type,
            m.route_name,
            m.route_path,
            m.component,
            m.redirect,
            m.query_param,
            m.is_frame,
            m.is_cache,
            m.menu_type_flag,
            m.visible,
            m.status,
            m.perms,
            m.icon,
            m.order_num,
            m.is_system,
            m.remark,
            m.create_time,
            m.update_time,
            m.create_by,
            m.update_by,
            m.is_deleted,
            m.delete_time
        FROM sys_menu m
        LEFT JOIN sys_role_menu rm ON m.id = rm.menu_id
        LEFT JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE m.is_deleted = 0 AND m.status = 1 AND ur.user_id = #{userId}
        ORDER BY m.parent_id ASC, m.order_num ASC
    </select>

    <!-- 根据角色ID查询菜单列表 -->
    <select id="selectMenusByRoleId" parameterType="Long" resultMap="SysMenuResult">
        SELECT
            m.id as menu_id,
            m.parent_id,
            m.menu_name,
            m.menu_code,
            m.menu_type,
            m.route_name,
            m.route_path,
            m.component,
            m.redirect,
            m.query_param,
            m.is_frame,
            m.is_cache,
            m.menu_type_flag,
            m.visible,
            m.status,
            m.perms,
            m.icon,
            m.order_num,
            m.is_system,
            m.remark,
            m.create_time,
            m.update_time,
            m.create_by,
            m.update_by,
            m.is_deleted,
            m.delete_time
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        WHERE m.is_deleted = 0 AND m.status = 1 AND rm.role_id = #{roleId}
        ORDER BY m.parent_id ASC, m.order_num ASC
    </select>

    <!-- 根据父菜单ID查询子菜单列表 -->
    <select id="selectChildrenByParentId" parameterType="Long" resultMap="SysMenuResult">
        SELECT
            m.id as menu_id,
            m.parent_id,
            m.menu_name,
            m.menu_code,
            m.menu_type,
            m.route_name,
            m.route_path,
            m.component,
            m.redirect,
            m.query_param,
            m.is_frame,
            m.is_cache,
            m.menu_type_flag,
            m.visible,
            m.status,
            m.perms,
            m.icon,
            m.order_num,
            m.is_system,
            m.remark,
            m.create_time,
            m.update_time,
            m.create_by,
            m.update_by,
            m.is_deleted,
            m.delete_time
        FROM sys_menu m
        WHERE m.parent_id = #{parentId} AND m.is_deleted = 0
        ORDER BY m.order_num ASC
    </select>

    <!-- 查询菜单树 -->
    <select id="selectMenuTree" resultMap="SysMenuResult">
        SELECT
            m.id as menu_id,
            m.parent_id,
            m.menu_name,
            m.menu_code,
            m.menu_type,
            m.route_name,
            m.route_path,
            m.component,
            m.redirect,
            m.query_param,
            m.is_frame,
            m.is_cache,
            m.menu_type_flag,
            m.visible,
            m.status,
            m.perms,
            m.icon,
            m.order_num,
            m.is_system,
            m.remark,
            m.create_time,
            m.update_time,
            m.create_by,
            m.update_by,
            m.is_deleted,
            m.delete_time
        FROM sys_menu m
        WHERE m.is_deleted = 0 AND m.status = 1
        ORDER BY m.parent_id ASC, m.order_num ASC
    </select>

    <!-- 根据用户ID查询权限列表 -->
    <select id="selectPermsByUserId" parameterType="Long" resultType="String">
        SELECT DISTINCT m.perms
        FROM sys_menu m
        LEFT JOIN sys_role_menu rm ON m.id = rm.menu_id
        LEFT JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE m.is_deleted = 0
          AND m.status = 1
          AND m.perms IS NOT NULL
          AND m.perms != ''
          AND ur.user_id = #{userId}
    </select>

</mapper>
